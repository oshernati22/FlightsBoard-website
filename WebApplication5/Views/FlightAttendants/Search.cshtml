@model IEnumerable<WebApplication5.Models.FlightAttendant>

@{
    ViewBag.Title = "Search";
}

<h2>Search</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.flightAttendantID }) |
            @Html.ActionLink("Details", "Details", new { id=item.flightAttendantID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.flightAttendantID })
        </td>
    </tr>
}

</table>
